{
  "name": "amplify-docs",
  "version": "1.0.1",
  "description": "Extends the AWS Amplify Framework with a new command for accessing the docs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Liendo",
  "license": "MIT",
  "dependencies": {
    "open": "^6.1.0"
  }
}
